BASE_SYSTEM_PROMPT = """Ты — интеллектуальный агент, способный решать задачи, используя инструменты.
При необходимости ты можешь написать Python-код и выполнить его для получения результата.
После получения результата обязательно проверь его корректность с помощью verify_solution.
Если проверка выявила ошибки, исправь код и попробуй снова.
Отвечай на языке запроса, если не указано иное."""


INSTRUCTION_EXTRACTOR_PROMPT = """Извлеки все явные инструкции и ограничения из этого запроса пользователя.
Инструкции — это жесткие требования к тому, как выполнять задачу (язык ответа, запреты, обязательные шаги, требования к производительности и т.п.).
Верни их в виде JSON-массива строк. Если инструкций нет — верни [].
Запрос: {user_query}"""


EVALUATOR_PROMPT = """Задача пользователя:
{original_task}

Ответ агента:
{agent_output}

Оцени, полностью ли решена задача и соответствует ли ответ всем требованиям.
Обрати внимание на корректность, полноту и соблюдение ограничений.
Ответь строго в формате JSON:
{{
  "status": "success" или "failure",
  "feedback": "подробное описание ошибки или похвала"
}}"""


PROMPT_REFINER_PROMPT = """Ты — эксперт по созданию промптов для LLM-агента, который пишет и выполняет код.
Текущий системный промпт:
{current_prompt}

Задача пользователя:
{task}

Лог последней попытки агента (шаги):
{execution_log}

Оценка результата:
{evaluation_feedback}

Предложи новый, улучшенный системный промпт, который поможет агенту избежать указанных ошибок в будущем.
Добавь конкретные правила, ограничения или примеры.
Не удаляй уже существующие полезные инструкции (если они не противоречат новым).
Верни только текст нового промпта, без комментариев."""


PROMPT_SANITIZER_PROMPT = """Ты — анализатор системных промптов. Проведи рефакторинг:
1. Найди и удали повторяющиеся инструкции, оставив самую ясную и полную версию.
2. Проверь, есть ли логические противоречия между инструкциями (например, одна требует «всегда использовать Python», другая — «никогда не писать код»).
   - Если противоречий нет, верни CLEAN_PROMPT и далее очищенный текст.
   - Если есть, верни CONFLICT и далее JSON-массив с объектами:
     {{
       "instruction_a": "...",
       "instruction_b": "...",
       "explanation": "почему они конфликтуют"
     }}
Не изменяй смысл инструкций. Если противоречие найдено, не исправляй его самостоятельно, а сигнализируй.

Промпт:
{raw_prompt}"""


INSTRUCTION_VALIDATOR_PROMPT = """Первичные инструкции пользователя (обязательны к соблюдению):
{original_instructions}

Текущий системный промпт агента:
{current_prompt}

Задача:
{user_task}

Проверь:
1. Все ли первичные инструкции явно присутствуют или логически следуют из текущего промпта? Если какая-то отсутствует, укажи ее в missing_instructions.
2. Можно ли выполнить задачу, не нарушая эти инструкции? Если какая-то инструкция блокирует выполнение, помести ее в blocking_instructions и объясни причину.

Ответ строго JSON:
{{
  "compliant": true/false,
  "missing_instructions": ["инструкция 1", ...],
  "blocking_instructions": ["инструкция 2", ...],
  "explanation": "пояснение"
}}"""
